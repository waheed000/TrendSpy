You previously built a React + Vite + Tailwind frontend for TrendSpy, but it's missing a few key files from the original plan, and the app currently shows a blank screen due to runtime errors.

Please add the missing files and fix all errors so the app runs perfectly.

## Missing files to create:

### 1. `src/utils/scoreColor.js`
Export a function that takes a winScore (0-100) and returns:
- `color`: 'red' for <50, 'yellow' for 50-74, 'green' for >=75
- `bgClass`: Tailwind background class (e.g., 'bg-red-100')
- `textClass`: Tailwind text class
- `borderClass`: Tailwind border class

### 2. `src/utils/formatPKR.js`
Export a function that takes a number and returns formatted PKR string (e.g., 12500 → "Rs. 12,500")

### 3. `src/utils/cityList.js`
Export an array of Pakistani cities with name, lat, lng, demandScore (mock). Include: Lahore, Karachi, Islamabad, Faisalabad, Rawalpindi, Multan, Peshawar, Quetta, Sialkot, Gujranwala.

### 4. `src/hooks/useProducts.js`
Custom hook that uses React Query to fetch products from the mock API. Should accept filters (city, category, minScore) and return { data, isLoading, error, refetch }.

### 5. `src/hooks/useTrends.js`
Custom hook that fetches trend data for a given productId. Returns line chart data for 30/60/90 days.

### 6. `.env` file in root
VITE_API_URL=http://localhost:5000
VITE_SOCKET_URL=http://localhost:5000

text

### 7. Fix blank screen issue
Check the following possible causes:
- Ensure `main.jsx` correctly imports `index.css` and renders `App`.
- Ensure `App.jsx` uses `BrowserRouter` and routes to `Landing` and `Layout` with protected routes.
- Ensure all components import React when needed (some may be missing `import React from 'react'`).
- Check `useSocket.jsx` – it should not return JSX, only a socket instance and a function to listen for events. Rewrite it as a proper hook that returns `{ isConnected, lastAlert }` and triggers toast notifications.
- Ensure `Layout.jsx` doesn't try to render `Sidebar` and `Navbar` before checking authentication (if user is null, redirect to login).

## Additional quality checks:
- Remove any remaining `hover:bg-white/8` invalid Tailwind classes (use `hover:bg-white/10` or `hover:bg-gray-100`).
- Ensure all mock data matches the shape expected by components (ProductCard expects product object with image, name, winScore, priceRange, trend, platforms, etc.)
- Add `node_modules` to `.gitignore` if not present.

After adding these files and fixes, run `npm run dev` and verify the app loads with no console errors. The landing page should be visible, and all routes should work.

Now implement all of the above.