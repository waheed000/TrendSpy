You are an expert DevOps engineer. Clean the folder structure of the Hunting Goals project and make it recruiter-ready — so anyone can clone, install, and run without errors.

## Goal

1. Rename `trendspy-backend/` → `backend/`
2. Ensure `frontend/` folder exists with all files
3. Root `package.json` has correct scripts to run both
4. Recruiter can clone and run in 3 steps

## New Folder Structure
Hunting-Goals/
├── frontend/
│ ├── src/
│ ├── public/
│ ├── index.html
│ ├── package.json
│ ├── vite.config.js
│ ├── tailwind.config.js
│ └── postcss.config.js
├── backend/
│ ├── server/
│ ├── models/
│ ├── controllers/
│ ├── middleware/
│ ├── services/
│ ├── scrapers/
│ ├── jobs/
│ ├── lib/
│ ├── data/
│ ├── Dockerfile
│ ├── package.json
│ └── .env.example
├── package.json
├── .gitignore
└── README.md

text

## Step 1: Rename Folder

```bash
mv trendspy-backend backend
Step 2: Update Root package.json
Create root package.json if not exists, or update with:

json
{
  "name": "hunting-goals",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "concurrently \"npm run dev:frontend\" \"npm run dev:backend\"",
    "dev:frontend": "cd frontend && npm run dev",
    "dev:backend": "cd backend && npm run dev",
    "install:all": "npm install && cd frontend && npm install && cd ../backend && npm install",
    "build": "cd frontend && npm run build && cd ../backend && npm run build"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
Step 3: Update Root README.md
markdown
# Hunting Goals — Pakistan's #1 Winning Product Hunter

## Quick Start (3 Steps)

```bash
# 1. Clone
git clone https://github.com/waheed477/Hunting-Goals.git
cd Hunting-Goals

# 2. Install all dependencies
npm run install:all

# 3. Run
npm run dev
Access
Frontend: http://localhost:5000

Backend API: http://localhost:3001/api/health

Tech Stack
Frontend: React + Vite + Tailwind CSS

Backend: Node.js + Express + MongoDB

text

## Step 4: Update `.gitignore`

```gitignore
# Dependencies
node_modules/
package-lock.json
yarn.lock

# Environment
.env
.env.local
.env.production

# Build
dist/
build/
.next/
out/

# OS
.DS_Store
Thumbs.db

# Logs
*.log
*.tmp
Step 5: Ensure Backend Imports Use Correct Paths
All imports in backend/ should use relative paths like ../models/User.js

No absolute imports that break after renaming

Step 6: Test Locally (After Changes)
bash
# Clone fresh
git clone <repo-url>
cd Hunting-Goals

# Install
npm run install:all

# Run
npm run dev
Expected Result
Frontend: http://localhost:5000 — loads without errors

Backend: http://localhost:3001/api/health — returns { status: "ok" }

No missing modules, no path errors

Now implement all of the above.

text
