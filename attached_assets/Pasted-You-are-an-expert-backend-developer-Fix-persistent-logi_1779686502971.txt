You are an expert backend developer. Fix persistent login and session management in TrendSpy.

## Problems to Fix:

### Problem 1: User logs out on browser close
Currently, when user closes browser and reopens, they need to login again. This is because:
- JWT token is stored in memory only
- No persistent storage for token

### Solution:
Store JWT token in localStorage (or httpOnly cookie with long expiry)

### Problem 2: Login should NOT require OTP after email verification
Already working correctly — users login with email + password only.

## Implementation Requirements:

### 1. Frontend: Store token in localStorage
Update `src/store/useAuthStore.js`:
- On login/verify, save token to localStorage
- On app load, check localStorage for existing token
- Auto-authenticate if token exists and is valid

### 2. Frontend: Token expiry check
- Check token expiry before auto-login
- If expired, clear localStorage and redirect to login

### 3. Backend: Ensure JWT has reasonable expiry
- JWT should expire after 7 days (already set)
- Cookie should have maxAge: 7 days

### 4. Logout functionality
- Clear localStorage
- Clear cookie
- Redirect to login

### 5. Auto-login on page refresh
- User should stay logged in after page refresh
- User should stay logged in after browser close/reopen (within 7 days)

## Files to Update:

### `src/store/useAuthStore.js`
```javascript
// Add these functions
const loadTokenFromStorage = () => {
  const token = localStorage.getItem('token');
  const user = localStorage.getItem('user');
  if (token && user) {
    set({ token, user: JSON.parse(user) });
    return true;
  }
  return false;
};

const persistToken = (token, user) => {
  localStorage.setItem('token', token);
  localStorage.setItem('user', JSON.stringify(user));
  set({ token, user });
};

const logout = () => {
  localStorage.removeItem('token');
  localStorage.removeItem('user');
  set({ token: null, user: null });
  // Also clear cookie if using httpOnly
  fetch('/api/auth/logout', { method: 'POST' });
};
src/App.jsx
javascript
// On app mount, check localStorage for existing session
useEffect(() => {
  const token = localStorage.getItem('token');
  const user = localStorage.getItem('user');
  if (token && user) {
    // Verify token with backend
    fetch('/api/auth/me', {
      headers: { 'Authorization': `Bearer ${token}` }
    }).then(res => {
      if (res.ok) {
        useAuthStore.setState({ token, user: JSON.parse(user) });
      } else {
        localStorage.clear();
      }
    });
  }
}, []);
Update Login.jsx and VerifyEmail.jsx
After successful login/verification, call persistToken(token, user)

Update Navbar.jsx
Logout button should call logout() function

Expected Behavior After Fix:
Action	Expected Result
User signs up → verifies OTP	✅ Logged in, token saved
User closes browser	✅ Token persists
User reopens browser	✅ Still logged in (no login prompt)
User refreshes page	✅ Still logged in
User clicks Logout	✅ Token cleared, redirected to login
User logs in after 8 days	❌ Token expired → login required again
Important:
Token expiry: 7 days from login/verification

Do NOT store sensitive data in localStorage (only token and basic user info)

Always validate token on backend for protected routes

Now implement all of the above with clean, production-ready code.