You are an expert backend developer. Build Phase 5 of TrendSpy backend with REAL implementations. NO MOCK DATA.

## Phase 5 Goal: Implement TikTok and News Scrapers only.

### Files to Create:

#### 1. lib/tiktokApi.js
Create a helper module that:
- Reads TIKTOK_RAPIDAPI_KEY from environment variables
- If missing, throws error: "❌ TIKTOK_RAPIDAPI_KEY missing. Add to .env.local"
- Creates a function fetchTrendingByHashtag(hashtag, count=10) that:
  - Calls RapidAPI TikTok endpoint
  - Returns video views, hashtag volume, product mentions
- Creates a function fetchTrendingFeed(region='PK') that:
  - Returns trending videos in Pakistan
- Uses axios with headers: X-RapidAPI-Key, X-RapidAPI-Host

#### 2. scrapers/tiktokScraper.js
Create a scraper that:
- Uses the tiktokApi helper module
- Targets hashtags: #pakistanshopping, #darazpakistan, #lahoreshopping, #karachishopping
- Extracts: video view count, hashtag volume, product mentions from captions
- Updates Product.tiktokViews and Product.tiktokHashtagVolume
- Returns array of products with social signals
- Handles API errors gracefully (logs error, returns empty array)

#### 3. scrapers/newsScraper.js (NO API KEY NEEDED)
Create a scraper that:
- Uses axios + cheerio
- Targets RSS feeds or HTML pages of:
  - Dawn Business: https://www.dawn.com/business/feed
  - ProPakistani: https://propakistani.pk/feed/
  - TechJuice: https://www.techjuice.pk/feed
- Searches for keywords: "import", "demand", "shortage", "market trend", "winning product", "surge"
- Extracts: headline, date, source URL, product mentioned
- No API key required. Works directly.
- Returns array of news signals (max 20 items)

#### 4. scrapers/index.js (UPDATE)
Export both scrapers:
```javascript
export { default as darazScraper } from './darazScraper.js';
export { default as olxScraper } from './olxScraper.js';
export { default as fbAdsScraper } from './fbAdsScraper.js';
export { default as googleTrendsScraper } from './googleTrendsScraper.js';
export { default as tiktokScraper } from './tiktokScraper.js';
export { default as newsScraper } from './newsScraper.js';
5. scripts/runPhase5Scrapers.js
Create a script that runs:

tiktokScraper()

newsScraper()

Logs success/failure for each

One failing does NOT stop the other

Environment Variables to Add (.env.local):
text
TIKTOK_RAPIDAPI_KEY=026e5a5624msh38152a75360d329p1b423ajsn54a1def63846
Important:
If TikTok API fails, log error but don't crash

News scraper must work without any key

No mock data anywhere

Now implement all of the above with clean, production-ready code.

text
