You are an expert QA and DevOps engineer. Your task is to audit and fix the Hunting Goals project so that ANY recruiter can clone it, install dependencies, set up `.env`, and run it WITHOUT ANY ERRORS.

## Target State

- Backend starts on `http://localhost:5001` (avoid port conflicts)
- Frontend starts on `http://localhost:5173`
- All API endpoints return correct JSON (no 500 errors)
- Registration and Login work perfectly
- No missing routes, no missing imports
- `.env.example` contains EVERY required variable

## Mandatory Fixes

### 1. Backend Port Conflict Prevention
- Set `PORT=5001` in `.env` and `server.js` fallback
- Kill any process on port 5000/5001 before starting

### 2. Fix Registration 500 Error
- Check `routes/auth.js` — ensure `bcryptjs` is imported
- Ensure `User` model is imported correctly
- Add proper `try/catch` with meaningful error messages

### 3. Add Missing Routes
- Ensure ALL routes from `server.js` have corresponding files in `routes/`
- Add a catch-all 404 handler that returns JSON (not HTML)

### 4. Validate `.env.example`
- Must include: `MONGODB_URI`, `JWT_SECRET`, `PORT`, `GROQ_API_KEY`, `FB_SESSION_COOKIE`, `EMAIL_USER`, `EMAIL_PASS`, `GREEN_API_INSTANCE_ID`, `GREEN_API_TOKEN`, `ADMIN_API_KEY`, `ALERTS_ENABLED`

### 5. Update README.md
- Clear 3-step setup: clone → install → run
- Mention `.env` setup with example
- Add troubleshooting for port conflicts

## Verification Checklist (Run After Fixes)

- [ ] `npm run dev` in backend → shows `Server running on port 5001`
- [ ] `curl http://localhost:5001/api/health` → returns `{"status":"ok"}`
- [ ] `curl -X POST http://localhost:5001/api/auth/register -H "Content-Type: application/json" -d '{"name":"Test","email":"test@example.com","password":"123456"}'` → returns `{"success":true}`
- [ ] Frontend `npm run dev` → shows `http://localhost:5173`
- [ ] Frontend can register a user without 500 error
- [ ] Frontend can login without JSON parse error

## Expected Final State

- Recruiter clones → `npm install` in both folders → creates `.env` → `npm run dev` → site works PERFECTLY
- NO 500 errors, NO 404s, NO missing imports

Now implement ALL fixes and verify with the checklist.