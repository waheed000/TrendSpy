You are an expert full-stack developer. Add an AI-Powered Ad Running Guide to the AI Report in Hunting Goals. This guide tells users exactly how to run ads for a product — target audience, budget, best time, ad copy variations, visual recommendations, and estimated results.

## Goal

When a user views the AI Report for a product, they see an "Ad Running Guide" section with actionable, city-specific ad advice based on real scraped ad data.

## Implementation

### 1. Update `services/groqService.js` – Generate Ad Guide

In the `analyzeProduct` function, add a new function `generateAdGuide()` and call it inside the main analysis:

```javascript
// Add this function to groqService.js
export async function generateAdGuide(productName, productData, city) {
  // Build a rich prompt with real ad data
  const advertiserCount = productData.advertiserCount || 0;
  const totalAds = productData.totalAds || 0;
  const maxDaysRunning = productData.maxDaysRunning || 0;
  const platforms = productData.platforms || ['Facebook'];
  const category = productData.category || 'General';
  
  // If we have Groq API key, use real AI
  if (groqClient) {
    try {
      const response = await groqClient.chat.completions.create({
        model: 'llama-3.3-70b-versatile',
        messages: [
          {
            role: 'system',
            content: `You are an expert Facebook & Instagram Ads strategist for the Pakistani market. Create a detailed ad running guide.`
          },
          {
            role: 'user',
            content: `
Create a complete ad running guide for this product:

Product: ${productName}
Category: ${category}
City: ${city || 'Lahore'}
Total active advertisers: ${advertiserCount}
Total ads running: ${totalAds}
Longest running ad: ${maxDaysRunning} days
Platforms: ${platforms.join(', ')}

Generate in JSON format:
{
  "targetAudience": {
    "locations": ["list of specific areas in the city"],
    "ageRange": "min-max",
    "gender": "All/Male/Female",
    "interests": ["interest1", "interest2"],
    "behaviors": ["behavior1", "behavior2"]
  },
  "budget": {
    "dailyBudget": "PKR amount",
    "totalBudget": "PKR amount",
    "strategy": "budget strategy text"
  },
  "bestTime": {
    "hours": "time range",
    "days": ["day1", "day2"],
    "avoid": "when to avoid"
  },
  "adCopyVariations": [
    { "headline": "headline text", "description": "description text", "cta": "Shop Now" }
  ],
  "visualRecommendations": {
    "creativeType": "video/image/carousel",
    "duration": "seconds if video",
    "colors": ["color1", "color2"],
    "textOverlay": "text to show on creative"
  },
  "estimatedResults": {
    "reach": "number range",
    "conversions": "number range",
    "roas": "x - y",
    "costPerConversion": "PKR amount"
  }
}`
          }
        ],
        temperature: 0.7,
        max_tokens: 1500,
      });
      
      // Parse JSON from Groq response
      const content = response.choices[0].message.content;
      const jsonMatch = content.match(/\{[\s\S]*\}/);
      if (jsonMatch) {
        const guide = JSON.parse(jsonMatch[0]);
        return { success: true, source: 'groq', guide };
      }
    } catch (error) {
      console.error('Groq ad guide error:', error);
    }
  }
  
  // Fallback: Local AI-generated guide (no API key needed)
  return {
    success: true,
    source: 'local',
    guide: generateLocalAdGuide(productName, productData, city)
  };
}

// Local fallback function
function generateLocalAdGuide(productName, productData, city) {
  const cityData = getCityData(city);
  const isHighScore = (productData.winScore || 0) > 75;
  const hasLongRunning = (productData.maxDaysRunning || 0) > 30;
  
  return {
    targetAudience: {
      locations: cityData?.areas || ['Main City'],
      ageRange: isHighScore ? '25-55' : '18-45',
      gender: 'All',
      interests: ['Online Shopping', 'Daraz.pk', 'OLX', 'Facebook Marketplace'],
      behaviors: ['Online Shoppers', 'Home Owners']
    },
    budget: {
      dailyBudget: isHighScore ? 'Rs. 1,200' : 'Rs. 800',
      totalBudget: isHighScore ? 'Rs. 36,000' : 'Rs. 24,000',
      strategy: `Start with Rs. 800/day and scale to ${isHighScore ? 'Rs. 1,500' : 'Rs. 1,200'}/day after 7 days`
    },
    bestTime: {
      hours: '6 PM - 10 PM',
      days: ['Thursday', 'Friday', 'Saturday'],
      avoid: 'Monday mornings'
    },
    adCopyVariations: [
      {
        headline: `🔥 ${productName} — Limited Time Offer!`,
        description: `Best ${productName} in ${city || 'Pakistan'}. Free delivery on all orders. Order now!`,
        cta: 'Shop Now'
      },
      {
        headline: `${productName} — Premium Quality at Best Price!`,
        description: `Get the best ${productName} in ${city || 'Pakistan'}. COD available. Limited stock!`,
        cta: 'Order Now'
      },
      {
        headline: `🎯 ${productName} — Don't Miss Out!`,
        description: `${hasLongRunning ? 'Trusted by 1000+ customers' : 'New arrival'}. Best ${productName} in ${city || 'Pakistan'}.`,
        cta: 'Learn More'
      }
    ],
    visualRecommendations: {
      creativeType: 'video',
      duration: '15-30 seconds',
      colors: ['Warm Orange', 'White', 'Teal'],
      textOverlay: `${productName} — ${isHighScore ? 'Best Seller' : 'New Arrival'}`
    },
    estimatedResults: {
      reach: isHighScore ? '50,000 - 70,000' : '30,000 - 50,000',
      conversions: isHighScore ? '200 - 300' : '100 - 200',
      roas: isHighScore ? '4x - 6x' : '3x - 4x',
      costPerConversion: isHighScore ? 'Rs. 150 - 200' : 'Rs. 200 - 300'
    }
  };
}

function getCityData(city) {
  const cities = {
    'Lahore': { areas: ['DHA', 'Gulberg', 'Johar Town', 'Model Town', 'Bahria Town'] },
    'Karachi': { areas: ['Clifton', 'Defence', 'Gulshan', 'Saddar', 'North Nazimabad'] },
    'Islamabad': { areas: ['F-7', 'F-8', 'G-10', 'E-11', 'I-8'] },
    'Rawalpindi': { areas: ['Saddar', 'Commercial Market', 'Westridge', 'Gulraiz'] },
    'Faisalabad': { areas: ['Clock Tower', 'Madina Town', 'Gulberg', 'Samanabad'] },
    'Multan': { areas: ['Kachehri Bazaar', 'Cantt', 'Shah Rukn-e-Alam', 'Bosan Road'] },
    'Peshawar': { areas: ['Saddar', 'University Road', 'Hayatabad', 'Faqirabad'] },
    'Quetta': { areas: ['Jinnah Road', 'Cantt', 'Satellite Town', 'Brewery Road'] }
  };
  return cities[city] || { areas: ['Main City'] };
}
2. Update app/api/ai/analyze/route.js – Return Ad Guide
javascript
import { generateAdGuide } from '@/services/groqService';

// Inside the analyze route, after getting product analysis:
const adGuide = await generateAdGuide(productName, productData, city);

// Add to response
return Response.json({
  success: true,
  data: {
    analysis: groqResult.analysis,
    profitCalculator: groqResult.profitCalculator,
    adCopy: groqResult.adCopy,
    suppliers: groqResult.suppliers,
    inventoryAdvice: groqResult.inventoryAdvice,
    sourcingLinks: groqResult.sourcingLinks,
    adGuide: adGuide.guide  // NEW: Ad Running Guide
  }
});
3. Update src/components/AIReportModal.jsx – Display Ad Guide
Add this section in the AI Report modal:

jsx
{/* Ad Running Guide */}
{data?.adGuide && (
  <div className="mt-6 p-4 bg-indigo-50 dark:bg-indigo-900/20 rounded-xl border border-indigo-200 dark:border-indigo-800">
    <h3 className="text-lg font-bold text-indigo-800 dark:text-indigo-300 mb-3 flex items-center gap-2">
      🎯 Ad Running Guide
    </h3>
    
    {/* Target Audience */}
    <div className="mb-4">
      <h4 className="font-semibold text-sm text-gray-700 dark:text-gray-300 mb-1">📍 Target Audience</h4>
      <div className="grid grid-cols-2 md:grid-cols-4 gap-2 text-sm">
        <span className="bg-white dark:bg-gray-800 px-3 py-1 rounded-lg border border-gray-200 dark:border-gray-700">
          {data.adGuide.targetAudience?.locations?.join(', ') || 'All City'}
        </span>
        <span className="bg-white dark:bg-gray-800 px-3 py-1 rounded-lg border border-gray-200 dark:border-gray-700">
          Age: {data.adGuide.targetAudience?.ageRange || '18-55'}
        </span>
        <span className="bg-white dark:bg-gray-800 px-3 py-1 rounded-lg border border-gray-200 dark:border-gray-700">
          {data.adGuide.targetAudience?.gender || 'All'}
        </span>
        <span className="bg-white dark:bg-gray-800 px-3 py-1 rounded-lg border border-gray-200 dark:border-gray-700">
          {data.adGuide.targetAudience?.interests?.slice(0, 2).join(', ') || 'Online Shoppers'}
        </span>
      </div>
    </div>

    {/* Budget */}
    <div className="grid grid-cols-1 md:grid-cols-3 gap-3 mb-4">
      <div className="bg-white dark:bg-gray-800 p-3 rounded-lg border border-gray-200 dark:border-gray-700">
        <p className="text-xs text-gray-500 dark:text-gray-400">Daily Budget</p>
        <p className="font-semibold text-indigo-600 dark:text-indigo-400">{data.adGuide.budget?.dailyBudget || 'Rs. 1,000'}</p>
      </div>
      <div className="bg-white dark:bg-gray-800 p-3 rounded-lg border border-gray-200 dark:border-gray-700">
        <p className="text-xs text-gray-500 dark:text-gray-400">Total Campaign</p>
        <p className="font-semibold text-indigo-600 dark:text-indigo-400">{data.adGuide.budget?.totalBudget || 'Rs. 30,000'}</p>
      </div>
      <div className="bg-white dark:bg-gray-800 p-3 rounded-lg border border-gray-200 dark:border-gray-700">
        <p className="text-xs text-gray-500 dark:text-gray-400">Best Time</p>
        <p className="font-semibold text-indigo-600 dark:text-indigo-400">{data.adGuide.bestTime?.hours || '6 PM - 10 PM'}</p>
      </div>
    </div>

    {/* Ad Copy Variations */}
    <div className="mb-4">
      <h4 className="font-semibold text-sm text-gray-700 dark:text-gray-300 mb-2">📢 Ad Copy Variations</h4>
      <div className="space-y-2">
        {data.adGuide.adCopyVariations?.map((copy, idx) => (
          <div key={idx} className="bg-white dark:bg-gray-800 p-3 rounded-lg border border-gray-200 dark:border-gray-700">
            <p className="font-medium text-gray-900 dark:text-white">{copy.headline}</p>
            <p className="text-sm text-gray-600 dark:text-gray-300">{copy.description}</p>
            <span className="inline-block mt-1 text-xs bg-indigo-100 dark:bg-indigo-900/40 text-indigo-700 dark:text-indigo-300 px-2 py-0.5 rounded">
              {copy.cta || 'Shop Now'}
            </span>
          </div>
        ))}
      </div>
    </div>

    {/* Estimated Results */}
    <div className="grid grid-cols-2 md:grid-cols-4 gap-2">
      <div className="bg-white dark:bg-gray-800 p-2 rounded-lg border border-gray-200 dark:border-gray-700 text-center">
        <p className="text-xs text-gray-500 dark:text-gray-400">Reach</p>
        <p className="font-semibold text-green-600 dark:text-green-400">{data.adGuide.estimatedResults?.reach || '50K-70K'}</p>
      </div>
      <div className="bg-white dark:bg-gray-800 p-2 rounded-lg border border-gray-200 dark:border-gray-700 text-center">
        <p className="text-xs text-gray-500 dark:text-gray-400">Conversions</p>
        <p className="font-semibold text-green-600 dark:text-green-400">{data.adGuide.estimatedResults?.conversions || '200-300'}</p>
      </div>
      <div className="bg-white dark:bg-gray-800 p-2 rounded-lg border border-gray-200 dark:border-gray-700 text-center">
        <p className="text-xs text-gray-500 dark:text-gray-400">ROAS</p>
        <p className="font-semibold text-green-600 dark:text-green-400">{data.adGuide.estimatedResults?.roas || '4x-6x'}</p>
      </div>
      <div className="bg-white dark:bg-gray-800 p-2 rounded-lg border border-gray-200 dark:border-gray-700 text-center">
        <p className="text-xs text-gray-500 dark:text-gray-400">Cost/Conversion</p>
        <p className="font-semibold text-green-600 dark:text-green-400">{data.adGuide.estimatedResults?.costPerConversion || 'Rs. 150'}</p>
      </div>
    </div>

    <p className="text-xs text-gray-500 dark:text-gray-400 mt-3">
      ℹ️ Based on {data.source === 'groq' ? 'real-time AI analysis' : 'local analysis'} of {productData.advertiserCount || 0} advertisers and {productData.totalAds || 0} active ads
    </p>
  </div>
)}
Testing Checklist
AI Report shows "Ad Running Guide" section

Target audience shows city-specific areas

Budget recommendations in PKR

3 ad copy variations displayed

Estimated results show reach, conversions, ROAS

With GROQ_API_KEY → real AI analysis

Without GROQ_API_KEY → local fallback

Expected Result
User sees a complete, actionable ad running guide in the AI Report

Guide is based on real scraped ad data

No manual research needed — user knows exactly how to run ads

Professional, clean UI in the dark/light theme

Now implement all of the above with clean, production-ready code.