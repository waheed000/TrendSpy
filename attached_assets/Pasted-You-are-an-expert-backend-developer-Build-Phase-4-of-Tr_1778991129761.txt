You are an expert backend developer. Build Phase 4 of TrendSpy backend completely with clean, professional, production-ready code.

## Phase 4 Goal: Implement Core Platform Scrapers

Implement scrapers for Daraz, OLX, Facebook Ad Library, and Google Trends. Each scraper must:
- Extract real data from the target platforms
- Save data to MongoDB (Product and ScrapedAd collections)
- Handle errors gracefully (no crashes)
- Use rotating user agents to avoid blocking
- Run on demand (will be connected to cron jobs in Phase 6)

## Packages to Install:
- cheerio
- puppeteer (for Facebook Ad Library)
- google-trends-api
- fake-useragent

## Files to Create:

### 1. lib/fakeUserAgent.js
Create a function that returns a random real user agent string from a list of 20+ common browsers (Chrome, Firefox, Safari on Windows/Mac/Linux).

### 2. lib/proxyService.js (placeholder for now)
Create a function getProxy() that returns null (will implement real proxy rotation later). Add TODO comments.

### 3. scrapers/darazScraper.js
Create a scraper that:
- Targets: daraz.pk best-sellers, flash sales, and search results for each category
- Extracts: product name, price (min/max), orders count, rating, image URL, category, platform "daraz"
- Uses cheerio + axios with fake user agent
- Returns array of products
- Saves/updates products in MongoDB (upsert by name + category)
- Updates lastScrapedAt timestamp
- Add delay between requests (1-2 seconds)
- Logs success/failure counts

### 4. scrapers/olxScraper.js
Create a scraper that:
- Targets: olx.com.pk for Lahore, Karachi, Islamabad, Faisalabad, Rawalpindi, Multan, Peshawar, Quetta, Sialkot, Gujranwala
- Extracts: listing title, price, views count (if available), city, date posted
- Detects spike: if listings increased by >50% in 7 days (store historical counts)
- Uses cheerio + axios
- Saves/updates products (if product matches existing, increment olxListings and olxViews)
- Return array of products with city-specific demand signals

### 5. scrapers/fbAdsScraper.js
Create a scraper that:
- Uses puppeteer (headless: true)
- Targets Facebook Ad Library for Pakistan (facebook.com/ads/library/?country=PK)
- Accepts search term (can iterate through categories)
- Extracts: ad headline, description, days running, creative type (image/video/carousel), advertiser name, platform "facebook"
- Only captures ads running 30+ days (proven profitable)
- Saves to ScrapedAd collection (upsert by adId)
- Uses slow and steady approach (wait for selectors, random delays)
- Returns array of ads found

### 6. scrapers/googleTrendsScraper.js
Create a scraper that:
- Uses google-trends-api npm package
- Fetches search volume trends for product keywords by city (10 Pakistani cities)
- Extracts: interest over time (last 30/60/90 days), related queries rising fast, percentage change
- Updates TrendScore collection with daily search volume data
- Also updates Product.googleTrendSpike field
- Returns summary of processed products

### 7. scrapers/index.js
Create an index file that exports all scrapers:
```javascript
export { default as darazScraper } from './darazScraper.js';
export { default as olxScraper } from './olxScraper.js';
export { default as fbAdsScraper } from './fbAdsScraper.js';
export { default as googleTrendsScraper } from './googleTrendsScraper.js';
8. scripts/runScrapers.js
Create a test script that:

Runs all 4 scrapers sequentially

Logs progress and results

Can be run manually with: node scripts/runScrapers.js

Additional Requirements:
All scrapers must export an async function that accepts optional parameters (category, city, searchTerm)

Each scraper must have its own error handling – one scraper failing should NOT crash others

Add console logs with timestamps for debugging

Use try-catch blocks for each major operation

Do NOT hardcode any credentials (use env variables if needed)

Respect robots.txt and add delays to avoid rate limiting (1-3 seconds between requests)

If Puppeteer fails, gracefully fallback to informing that Facebook scraping needs manual cookie/session (add TODO note)

Testing Checklist:
After implementation, verify:

darazScraper() returns array of products with valid data

olxScraper() returns array with city-specific demand signals

fbAdsScraper() (may need manual cookie – if fails, add note for Phase 5)

googleTrendsScraper() returns trend data and updates TrendScore collection

Data is saved correctly in MongoDB (products and scrapedAds collections)

Important Note:
Facebook Ad Library scraper may require additional setup (logged-in session). If it fails, implement a mock version for now with a clear TODO comment. We will complete it in Phase 5 with better approach.

Now implement all of the above with clean, production-ready code. Focus on making each scraper robust and error-resistant. Add comments explaining complex logic.