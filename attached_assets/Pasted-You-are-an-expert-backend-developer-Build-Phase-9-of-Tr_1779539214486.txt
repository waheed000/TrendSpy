You are an expert backend developer. Build Phase 9 of TrendSpy backend with clean, professional, production-ready code. NO DUMMY DATA. All implementations must be real and production-ready.

## Phase 9 Goal: AI Analyst (Groq) + TikTok (fits-api) + Final Integration

Implement Groq API for AI product analysis and replace RapidAPI TikTok with fits-api (no API key needed).

## Packages to Install:
- groq-sdk
- fits-api
- playwright (for fits-api)

## Files to Create and Update:

### 1. Replace TikTok Scraper with fits-api

#### scrapers/tiktokScraper.js (REWRITE)
Create new TikTok scraper using fits-api (NO API KEY NEEDED):
- Import fits-api: `import { getUserVideos, getHashtagVideos } from 'fits-api'`
- Fetch trending videos for Pakistan region
- Fetch videos for hashtags: #pakistanshopping, #darazpakistan, #lahoreshopping, #karachishopping
- Extract: video view count, comment count, share count, product mentions from captions and hashtags
- Update Product.tiktokViews and Product.tiktokHashtagVolume
- Add random delays between requests (1-3 seconds)
- Handle errors gracefully (if fits-api fails, log error and return empty array)
- No API key required

### 2. Groq AI Service

#### services/groqService.js (NEW)
Create Groq AI service that:
- Reads GROQ_API_KEY from environment variables
- If missing, throws clear error: "❌ GROQ_API_KEY missing. Get it from https://console.groq.com"
- Uses groq-sdk with model "llama-3.3-70b-versatile" (free tier)
- Functions to implement:
  
  `analyzeProduct(productName, productData)` - returns JSON object:
  ```json
  {
    "profitAnalysis": {
      "buyPrice": "suggested price from Alibaba/Daraz",
      "sellPrice": "recommended selling price in PKR",
      "profitMargin": "calculated margin percentage",
      "recommendedPlatform": "Daraz/OLX/TikTok/Shopify"
    },
    "adCopy": {
      "english": "English ad copy for Facebook/Instagram",
      "urdu": "Roman Urdu ad copy for TikTok"
    },
    "competitorAlert": "Number of competitors and warning message",
    "marketPotential": "High/Medium/Low with explanation"
  }
generateAdCopy(productName, category, targetAudience) - returns ad copy in English + Roman Urdu

getSeasonalRecommendation(currentSeason) - returns products to stock for upcoming season

app/api/ai/analyze/route.js (NEW)
POST endpoint (protected)

Body: { productName, productId (optional) }

If productId provided, fetch product from database

Call groqService.analyzeProduct()

Return AI analysis JSON

Rate limit: 10 requests per user per hour

app/api/ai/adcopy/route.js (NEW)
POST endpoint (protected)

Body: { productName, category, targetAudience }

Call groqService.generateAdCopy()

Return { english, urdu } ad copy

3. Seasonal Engine Integration
services/seasonalService.js (UPDATE)
Already exists from Phase 6. Update to:

Read seasonal products from database (SeasonalEvent model)

Recommend products based on upcoming season (Ramadan, Eid, Winter, Wedding, Summer, Back to School)

Export: getSeasonalRecommendations(limit=10) - returns products with high seasonalRelevance

Export: getUpcomingSeasonEvents() - returns next 2 seasons with countdown

app/api/seasonal/recommendations/route.js (NEW)
GET endpoint (public)

Query params: limit (default 10)

Returns products with seasonalRelevance >= 70

Includes current season and upcoming season info

app/api/seasonal/events/route.js (NEW)
GET endpoint (public)

Returns all seasonal events with dates, preparation start dates, category boosts

4. Dashboard Stats API
app/api/stats/route.js (NEW)
GET endpoint (protected)

Returns real-time stats from database:

json
{
  "totalProducts": "count from Product collection",
  "winningProducts": "count where winScore >= 75",
  "activeAlerts": "count of active alerts",
  "alertsSentToday": "count from logs (placeholder for now)",
  "scrapers": {
    "lastDarazRun": "timestamp",
    "lastOLXRun": "timestamp",
    "lastTikTokRun": "timestamp"
  },
  "trendingCategories": ["Electronics", "Fashion", "Beauty"],
  "topCities": ["Lahore", "Karachi", "Islamabad"]
}
5. Update Alert Service with Groq Integration
services/alertService.js (UPDATE)
When sending high-value alerts (winScore >= 85), optionally include AI-generated summary

Add field aiSummary to alert message (short one-line insight)

6. Environment Variables (.env.local)
text
GROQ_API_KEY=your_groq_api_key_here
# TikTok - no key needed (fits-api)
# RapidAPI key no longer needed - can remove TIKTOK_RAPIDAPI_KEY
7. Package.json Updates
json
{
  "dependencies": {
    "groq-sdk": "latest",
    "fits-api": "latest"
  },
  "devDependencies": {
    "playwright": "latest"
  }
}
8. Remove/Deprecate
Remove TIKTOK_RAPIDAPI_KEY from env (not needed anymore)

Old tiktokApi.js helper can be deleted. 
fits-api Setup Instructions:
bash
npm install fits-api
npx playwright install chromium
No API key needed. Works out of the box.

Testing Checklist:
TikTok scraper runs without API key

POST /api/ai/analyze returns AI product analysis

POST /api/ai/adcopy returns Urdu + English ad copy

GET /api/seasonal/recommendations returns products

GET /api/stats returns real dashboard stats

Important Notes:
Groq free tier: 30 requests per minute (enough for testing)

fits-api is self-hosted, no rate limits

All scrapers must continue working without TikTok API key

Production deployment: Ensure GROQ_API_KEY is set on Render/Replit

Now implement all of the above. NO DUMMY DATA. All implementations must be real and production-ready.