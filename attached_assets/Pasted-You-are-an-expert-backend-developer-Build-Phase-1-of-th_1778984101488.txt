You are an expert backend developer. Build Phase 1 of the TrendSpy backend using Next.js (App Router) with MongoDB.

## Goal
Set up the Next.js project, install all required dependencies, create the folder structure, connect to MongoDB (local Compass), and verify the connection works.

## Project Structure to Create (inside a new Next.js project)
trendspy-backend/
├── app/
│ └── api/
│ └── health/
│ └── route.js (test API to check DB connection)
├── lib/
│ └── db.js (MongoDB connection)
├── models/ (empty for now, will add in Phase 2)
├── services/ (empty)
├── scrapers/ (empty)
├── jobs/ (empty)
├── middleware/ (empty)
├── .env.local
├── next.config.js
├── package.json
└── jsconfig.json (for path aliases)

text

## Step 1: Initialize Next.js
Run these commands in the Replit terminal:
```bash
npx create-next-app@latest trendspy-backend --typescript --tailwind --app --no-src-dir --turbopack
cd trendspy-backend
But if Replit already has a Next.js template, just ensure package.json has the correct dependencies.

Step 2: Install Required Packages
Add to package.json dependencies:

json
{
  "dependencies": {
    "mongoose": "^8.0.0",
    "dotenv": "^16.3.1",
    "next": "14.0.4",
    "react": "^18",
    "react-dom": "^18"
  }
}
Then run npm install.

Step 3: Create .env.local
env
MONGODB_URI=mongodb://localhost:27017/trendspy
DB_NAME=trendspy
NODE_ENV=development
PORT=3000
Step 4: Create lib/db.js
This file handles MongoDB connection with caching to avoid multiple connections in development.

javascript
import mongoose from 'mongoose';

const MONGODB_URI = process.env.MONGODB_URI;
const DB_NAME = process.env.DB_NAME;

if (!MONGODB_URI) {
  throw new Error('Please define MONGODB_URI environment variable');
}

let cached = global.mongoose;

if (!cached) {
  cached = global.mongoose = { conn: null, promise: null };
}

export async function connectDB() {
  if (cached.conn) {
    return cached.conn;
  }

  if (!cached.promise) {
    const opts = {
      dbName: DB_NAME,
      bufferCommands: false,
    };

    cached.promise = mongoose.connect(MONGODB_URI, opts).then((mongoose) => {
      console.log('✅ MongoDB connected successfully to', DB_NAME);
      return mongoose;
    }).catch((err) => {
      console.error('❌ MongoDB connection error:', err);
      throw err;
    });
  }

  cached.conn = await cached.promise;
  return cached.conn;
}
Step 5: Create app/api/health/route.js
A test endpoint that checks database connection.

javascript
import { connectDB } from '@/lib/db';

export async function GET() {
  try {
    await connectDB();
    return Response.json({
      status: 'ok',
      message: 'MongoDB connected successfully',
      timestamp: new Date().toISOString(),
      database: process.env.DB_NAME
    });
  } catch (error) {
    console.error('Health check failed:', error);
    return Response.json(
      {
        status: 'error',
        message: 'Database connection failed',
        error: error.message
      },
      { status: 500 }
    );
  }
}
Step 6: Create jsconfig.json
For path aliases (@/ imports).

json
{
  "compilerOptions": {
    "baseUrl": ".",
    "paths": {
      "@/*": ["./*"]
    }
  }
}
Step 7: Update next.config.js
javascript
/** @type {import('next').NextConfig} */
const nextConfig = {
  experimental: {
    serverComponentsExternalPackages: ['mongoose']
  },
  webpack: (config) => {
    config.externals = [...config.externals, 'mongoose'];
    return config;
  }
};

module.exports = nextConfig;
Step 8: Create a simple test script (optional)
Create scripts/test-db.js to verify connection from terminal:

javascript
// Run with: node scripts/test-db.js
require('dotenv').config({ path: '.env.local' });
const mongoose = require('mongoose');

async function testConnection() {
  try {
    await mongoose.connect(process.env.MONGODB_URI, {
      dbName: process.env.DB_NAME
    });
    console.log('✅ Direct connection successful');
    console.log('Database:', mongoose.connection.name);
    await mongoose.disconnect();
  } catch (err) {
    console.error('❌ Connection failed:', err.message);
  }
}

testConnection();
Step 9: Update package.json scripts
json
"scripts": {
  "dev": "next dev",
  "build": "next build",
  "start": "next start",
  "test:db": "node scripts/test-db.js"
}
Step 10: Verify Everything Works
After creating all files, run:

bash
npm run dev
Then visit http://localhost:3000/api/health – you should see:

json
{
  "status": "ok",
  "message": "MongoDB connected successfully",
  "timestamp": "...",
  "database": "trendspy"
}
Also run npm run test:db to confirm direct connection.