You are an expert frontend developer. Enhance the Landing page with a premium light mode background that adds visual depth — a subtle gradient mesh + soft geometric shapes. No heavy images, no slow loading.

## Goal

Add a clean, professional background that:
- Uses a soft gradient mesh (light blue to white)
- Includes subtle floating geometric shapes (circles, blobs)
- Matches the Deep Blue (#1e3a5f) + Ocean Teal (#0d9488) theme
- Does NOT distract from the content
- Loads fast (CSS-only, no image assets)

## Implementation

### Update `src/pages/Landing.jsx`

Add background elements inside the main container:

```jsx
export default function Landing() {
  return (
    <div className="relative min-h-screen overflow-hidden bg-gradient-to-br from-blue-50/80 via-white to-teal-50/40">
      
      {/* Background Decorations — CSS-only, no images */}
      <div className="absolute inset-0 pointer-events-none">
        {/* Large gradient blob — top right */}
        <div className="absolute -top-40 -right-40 w-[600px] h-[600px] rounded-full bg-gradient-to-br from-primary/5 to-secondary/10 blur-3xl" />
        
        {/* Medium blob — bottom left */}
        <div className="absolute -bottom-40 -left-40 w-[500px] h-[500px] rounded-full bg-gradient-to-tr from-secondary/5 to-primary/8 blur-3xl" />
        
        {/* Small accent circle — near hero */}
        <div className="absolute top-1/3 left-1/4 w-12 h-12 rounded-full bg-secondary/10 blur-xl" />
        
        {/* Another small accent */}
        <div className="absolute bottom-1/4 right-1/3 w-8 h-8 rounded-full bg-primary/8 blur-xl" />
        
        {/* Subtle grid pattern overlay (very light) */}
        <div className="absolute inset-0 bg-[url('data:image/svg+xml,%3Csvg width="60" height="60" viewBox="0 0 60 60" xmlns="http://www.w3.org/2000/svg"%3E%3Cg fill="none" fill-rule="evenodd"%3E%3Cg fill="%231e3a5f" fill-opacity="0.03"%3E%3Cpath d="M36 34v-4h-2v4h-4v2h4v4h2v-4h4v-2h-4zm0-30V0h-2v4h-4v2h4v4h2V6h4V4h-4zM6 34v-4H4v4H0v2h4v4h2v-4h4v-2H6zM6 4V0H4v4H0v2h4v4h2V6h4V4H6z"/%3E%3C/g%3E%3C/g%3E%3C/svg%3E')] bg-repeat opacity-40" />
      </div>

      {/* Content — everything stays above the background */}
      <div className="relative z-10 container mx-auto px-4 py-12">
        {/* Your existing landing content here */}
      </div>
    </div>
  );
}
Add Subtle Card Shadows
Update card styles to feel elevated but clean:

css
.card-premium {
  background: rgba(255, 255, 255, 0.7);
  backdrop-filter: blur(4px);
  border: 1px solid rgba(255, 255, 255, 0.5);
  box-shadow: 0 8px 32px rgba(30, 58, 95, 0.06);
  transition: box-shadow 0.2s ease;
}

.card-premium:hover {
  box-shadow: 0 12px 48px rgba(30, 58, 95, 0.10);
}
Expected Result
Landing page now has a soft, premium background with depth

Colors stay in the Deep Blue + Ocean Teal family

Content is easy to read (good contrast)

No heavy images — fast loading

Light mode feels polished and professional

Now implement all of the above with clean, production-ready code.